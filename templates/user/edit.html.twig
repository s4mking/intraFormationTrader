{% extends 'base.html.twig' %}

{% block title %}Votre compte{% endblock %}
{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('login') }}
{% endblock %}
{% block body %}

    {{ form_start(accountForm, {'attr': {'class': 'form-signin'}}) }}
    <h3>Votre compte</h3>
    {{ user.email }}
    {{ form_widget(accountForm) }}
    {{ form_end(accountForm) }}

{% endblock %}
